algorithm to optimize in the federated setting in Section 4.2. 4.1 The ∆-FL Framework The ∆-FL framework aims to improve the performance of the tail clients by minimizing the superquantile of the loss distribution. Given a discretization θ ,...,θ of (0,1], ∆-FL maintains r models w ,...,w , one 1 r 1 r for each tail threshold θ . We allow each test{client to s}elect the best model w w ,...,w , according to j 1 r its local data. Recall the schematic in Figure 1 for an illustration. ∈{ } For a given tail threshold θ, we propose to minimize the (1 θ)-superquantile of the distributions of losses: − (cid:20) (cid:21) min F θ (w):=Sθ (cid:0) F 1 (w),...,F n (w) (cid:1) + λ w 2 . (6) w∈Rd 2(cid:107) (cid:107) The objective (6) focuses on poor-performing clients — specifically those with performance worse than the (1 θ)-quantile of the distribution of losses (F (w),...,F (w)). In contrast, the vanilla FL objective 1 n − 7 1 E[Z] Q (Z) θ 1 0 Sθ (Z)=E[Z Z >Q θ (Z)] | 1 Figure 2: Left:(1−θ)-quantile Q θ (Z) and superquantile Sθ (Z) of a continuous r.v.Z. Right: The set of feasible mixture weights π=(π ,π ,π )∈P in the dual formulation (5) is given by the intersection of the box constraints 1 2 3 θ 0≤π ≤(3θ)−1 for i=1,2,3, with the simplex constraint π +π +π =1. i 1 2 3 optimizes (1/n) (cid:80)n F (w)+λ/2 w 2, which is lim F (w); this equally weights all clients involved in training. At the ot i h = e 1 r e i xtreme θ (cid:107) 0 (cid:107), we recover the θ w → o 1 rst θ -case loss over all clients. → Distributionally Robust Interpretation. We have the following dual characterization of ∆-FL as a distributionally robust learning objective, as a consequence of the dual representation (5) of the superquantile. Property 1. The ∆-FL objective (6) can also be written as n (cid:88) F (w)= max π F (w), θ π∈Pθ i=1 i i (7) where, := (cid:8) π ∆n−1 : π (nθ)−1for all i [n] (cid:9) . θ i P ∈ ≤ ∈ This reformulation shows that ∆-FL can be interpreted as a distributionally robust variant of the vanilla FL objective: since (cid:80)n π F (w) = F(w;p ) is loss of w on the mixture p = (cid:80)n π q of the training i=1 i i π π i=1 i i distributions q ,...,q , we get that ∆-FL aims to minimize the worst-case loss over all mixtures p subject 1 n π to the constraint that π (nθ)−1. i This formulation also≤reveals two important properties of the ∆-FL objective. First, we note that the objective F , as a max function. is convex whenever the losses F are convex. Second, it is a non-smooth θ i function, with the non-smoothness stemming from the maximum over the polytope (cf. Figure 2). These θ two properties will play important role in the convergence analysis of our federated aPlgorithm in Section 5.1. 4.2 Federated Optimization for ∆-FL We now propose a federated optimization algorithm for the ∆-FL objective (6). While there could be many approaches to optimizing (6), we consider algorithms similar to FedAvg for their ability to avoid communication bottlenecks and preserve the privacy of user data. Owing to the tail mean interpretation of the superquantile (Figure 2), a natural algorithm to minimize it first evaluates the loss on all the clients and only performs gradient updates on those clients in the tail above the (1 θ)-quantile. However, since a practical algorithm cannot assume that all the clients are available at a giv−en time, we perform the same operation on a subsample of clients. The optimization algorithm for the ∆-FL objective (6) is given in Algorithm 1. It has the following four steps: (a) Model Broadcast (line 2): The server samples a set S of m clients from [n] and sends the current model w(t). (b) Quantile Computation and Reweighting (lines 3 and 5): Selected clients i S and the server collaborate to estimate the (1 θ)-quantile of the losses F (w(t)) with differential pri∈vacy. The clients then update i − 8 Algorithm 1 The ∆-FL Algorithm Input: Initial iterate w(0), number of communication rounds T, number of clients per round m, number of local updates τ, local step size γ 1: for t=0,1,...,T 1 do 2: Sample m clien−ts from [n] without replacement in S 3: Estimate the (1 θ)-quantile of F i (w(t)) for i S with distributed differential privacy (Algorithm 2); call this Q(t − ) ∈ 4: for each selected client i S in parallel do 5: Set π˜ i (t) = I (cid:0) F i (w(t)) ∈ ≥ Q(t) (cid:1) 6: Initialize w(t)