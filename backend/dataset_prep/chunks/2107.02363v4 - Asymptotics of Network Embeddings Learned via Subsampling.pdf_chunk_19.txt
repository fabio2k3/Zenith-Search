0 n ns n 26 Asymptotics of Network Embeddings Learned via Subsampling Proposition 24 Suppose that Assumption 1 holds. Then for Algorithm 2, Assumptions 4 and 5 hold with  2k  if a = 1,  E ρ ij W n f (λ ,λ ,a ) = n i j ij   2kl (cid:8) W(λ ,·)W(λ ,·)α+W(λ ,·)W(λ ,·)α(cid:9) if a = 0;  i j j i ij E E (α) W W with s = (log(n)/nρ )1/2, E[f2] = O(ρ−1), and β = β min{α,1}. n n n n W Alternatively to using a unigram distribution for negative sampling, one other approach is to select edges (such as via uniform sampling as above), and then return the induced subgraph as the entire sample. Algorithm 3 (Uniform edge sampling and induced subgraph negative sampling) Given a graph G and number of edges k to sample, we perform the following steps: n i) Form S (G ) by sampling k edges from G uniformly and without replacement; 0 n n ii) Return S(G ) as the induced subgraph formed from all of the vertices u ∈ V(S (G )). n 0 n Proposition 25 Suppose that Assumption 1 holds. Then for Algorithm 3, Assumptions 4 and 5 hold with  4k 4k(k−1)W(λ ,·)W(λ ,·)    E ρ + E2 i j if a ij = 1, f (λ ,λ ,a ) = W n W n i j ij 4k(k−1)W(λ ,·)W(λ ,·)  i j  if a = 0;   E2 ij W with s = (log(n)/nρ )1/2, β = β , and E[f2] = O(ρ−1). n n W n n Wecanalsoconsiderrandomwalkbasedsamplingschemes(seee.g.Perozzietal.,2014). Algorithm 4 (Random walk sampling with unigram negative sampling) Given a graph G , a walk length k, number of negative samples l per positively sampled vertex, n unigram parameter α and an initial distribution π (·|G ), we 0 n i) Select an initial vertex v˜ according to π ; 1 0 ii) Perform a simple random walk on G of length k to form a path (v˜) , and report n i i≤k+1 (v˜,v˜ ) for i ≤ k as part of S (G ); i i+1 0 n iii) For each vertex v˜, we select l vertices (η ) independently and identically according i j j≤l to the unigram distribution P(cid:0) v˜ = v for some i ≤ k|G (cid:1)α i n Ug (v|G ) = α n (cid:80) P(cid:0) v˜ = u for some i ≤ k|G (cid:1)α u∈Vn i n and then form S (G ) as the collection of (v˜,η ) which are non-edges in G ; ns n i j n 27 Davison and Austern and then return S(G ) = S (G )∪S (G ). n 0 n ns n In the above scheme, there is freedom in how we can specify the initial vertex of the random walk. Here we will do so using the stationary distribution of a simple random walk on G , namely π (v|G ) = deg (v)/2E , as this simplifies the analysis of the scheme. n 0 n n n Proposition 26 Suppose that Assumption 1 holds. Then for Algorithm 3 with choice of initial distribution π (v|G ) = deg (v)/2E , Assumptions 4 and 5 hold with 0 n n n  2k  if a = 1,  E ρ ij W n f (λ ,λ ,a ) = n i j ij   l(k+1) (cid:8) W(λ ,·)W(λ ,·)α+W(λ ,·)W(λ ,·)α(cid:9) if a = 0;  i j j i ij E E (α) W W with s = (log(n)/nρ )1/2, E[f2] = O(ρ−1), and β = β min{α,1}. n n n n W One important property of the samplers discussed in Algorithms 2, 3 and 4 is that they are essentially invariant to the scale of the underlying graph, in that the dominating parts of the expressions for the f˜ (l,l(cid:48),x) are free of the sparsity factor ρ . We write this down n n for the random walk sampler. Lemma 27 For Algorithm 4, under the conditions of Proposition 26 we get that 2kW(λ ,λ ) f˜ (λ ,λ ,1) = i j n i j E W f˜ (λ ,λ ,0) = l(k+1) (cid:8) W(λ ,·)W(λ ,·)α+W(λ ,·)αW(λ ,·) (cid:9) ·(1−ρ W(λ ,λ )). n i j i j i j n i j E E (α) W W In particular, we have that f˜ (λ ,λ ,1) is free of ρ , and n i j n f˜ (λ ,λ ,0) = l(k+1) (cid:8) W(λ ,·)W(λ ,·)α+W(λ ,·)αW(λ ,·) (cid:9) ·(1+O(ρ )) n i j i j i j n E E (α) W W Remark 28 We note that in algorithmic implementations of negative sampling schemes in practice,