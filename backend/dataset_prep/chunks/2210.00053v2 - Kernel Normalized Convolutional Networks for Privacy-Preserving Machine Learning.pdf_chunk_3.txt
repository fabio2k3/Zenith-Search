each the combination of the KernelNorm and convolutional layer, sample in the batch. The normalization unit of GroupNorm where the output of the former is given as input to the latter. [20] contains all elements in the height and width dimensions The modern CNNs are batch-normalized, leveraging the similar to LayerNorm, but a subset of elements (specified by BatchNormandconvolutionallayersintheirarchitectures.The the group size) in the channel dimension. corresponding layer/group-normalized networks are obtained BatchNorm,LayerNorm,andGroupNormarereferredtoas bysimplyreplacingBatchNormwithLayerNorm/GroupNorm. globalnormalizationlayersbecausetheyconsiderallelements The kernel-normalized counterparts [21], on the other hand, intheheightandwidthdimensionsduringnormalization[34]. employ the KernelNorm and KNConv layers as the main There is also a one-to-one correspondence between the input building blocks, while forgoing the BatchNorm layers. For and output elements in the aforementioned layers, implying more details on the normalization layers, the readers can see thattheydonotmodifytheinputshape[21].Theselayershave [16], [19]–[21]. III. EVALUATION 4) CIFAR-100-PreactResNet-18 (cross-device FL): Akin to the cross-silo counterpart, but in a cross-device fedWe conduct extensive experiments to investigate the pereratedsettingconsistingof100clients,where20clients formance of different batch-independent normalization layers are randomly chosen by the server in each round. including LayerNorm, GroupNorm, and KernelNorm in the 5) CIFAR-10-ResNet-8 (DP): The goal of this case study cross-silo and cross-device FL as well as DP and DP-FL is to train the shallow ResNet-8 model on the lowenvironments.Inthefollowing,wefirstprovidethedescription resolution CIFAR-10 dataset in the DP environment. of the datasets, models, and case studies, and then discuss the 6) CIFAR-10-DenseNet-20×16 (DP): This case study results and findings. aims to train the deeper DenseNet-20×16 model on the low-resolution CIFAR-10 dataset in the DP setting. A. Experimental Setup 7) Imagenette-PreactResNet-18(DP).Thepurposeofthis Datasets. The CIFAR-10/100 dataset [29] contains 50000 casestudyistotrainthedeeper PreactResNet-18model train and 10000 test samples of shape 32×32 from 10/100 on the medium-resolution Imagenette dataset in the classes. The Imagenette dataset (160-pixel version) [30] is a differentially private environment. subsetofImagenet[36],including9469trainand3925valida8) CIFAR-10-VGG-6 (DP-FL): This case study aims to tionimagesfrom10”easilyclassified”labels.Thefeaturevaltrain the VGG-6 model on the CIFAR-10 dataset in a uesaredividedby255forKernelNormbasedmodels,whereas differentially private federated setting with 10 clients, they are normalized using the mean and standard deviation where the clients have samples from 4 classes. The of CIFAR-10/100 or ImageNet for NoNorm, LayerNorm, and sample sizes of the clients are highly similar. GroupNorm based counterparts. The samples of Imagenette 9) CIFAR-10-ResNet-8 (DP-FL): Similar to the previous are resized to 128×128. case study, but with ResNet-8 as the model. Models. We adopt the VGG-6 architecture from [27], Federated training. We employ five different values for ResNet-8 model from [21], PreactResNet-18 implementalearning rate tuning in the federated case studies: η={0.005, tion from [37], and DenseNet-20×16 (depth of 20 and 0.01, 0.025, 0.05, 0.1}. The KernelNorm based models are growth rate of 16) implementation from [38]. In layer/grouptrainedfor400and1000communicationroundsintheCIFARnormalized networks, BatchNorm is substituted by Layer10 and CIFAR-100 case studies, respectively. The number of Norm/GroupNorm. In the NoNorm case, the BatchNorm layrounds for the NoNorm, LayerNorm, and GroupNorm based ers are either removed or replaced with the identity layer. models is as twice as the kernel normalized counterparts The kernel-normalized counterparts are implemented by redue to their slower convergence rate. The group size is the moving the BatchNorm layers, replacing the convolutional defaultvalueof32fortheGroupNormlayer[20].Thedropout layerswithKNConv,andinsertingaKernelNormlayerbefore probability for KNConv and KernelNorm layers are 0.1 and the final average-pooling layer in the ResNet, PreactResNet, 0.5, respectively. The loss function is cross-entropy, optimizer and DenseNet models. In FL, the models employ the ReLU is SGD with momentum of zero, and training algorithm is activation. In DP, on the other hand, the activation function FedAvg with number of local epochs of 1. is Mish [39], which was successfully used in [24] to achieve Differentiallyprivatetraining.Wesetε=6.0andδ =10−5 SOTA accuracy. We implement the models in the PyTorch for all DP case studies. Regarding parameter tuning, we use library (version 1.11) [40]. learning rate values of η={1.0, 1.5, 2.0} and clipping values CaseStudies.Wedesignninedifferentcasestudies(fourin of C={1.0, 1.5, 2.0}. The ResNet-8, DenseNet-20×16, and FL, three in DP, and two in DP-FL) to make the performance PreactResNet-18modelsaretrainedfor50,70,and70epochs, comparison among the normalization layers: respectively.Thelearningrateisdividedby2atepochs(T-30) 1) CIFAR-10-VGG-6 (cross-silo FL): This case study and (T-10), where T is the number of epochs (i.e. 50 or 70). aims to train the shallow VGG-6 model on the low- ThegroupsizeofGroupNormis16forDenseNet-20×16,but resolution CIFAR-10 dataset in a cross-silo federated 32 for the other models. Notice that we cannot set group size environmentcontaining10clients,whereeachclienthas to 32 for DenseNet-20×16 because the number of channels samples from only 2 classes. The sample sizes of the must be divisible by the group size. The dropout probability clients are almost the same. is 0.1 for all KNConv layers in the kernel normalized models. 2) CIFAR-10-VGG-6 (cross-device FL): Similar to the For ResNet-8, the dropout probability of KernelNorm is 0.25, cross-silo counterpart, but in a cross-device federated whereas it is 0.5 for DenseNet-20×16 and PreactResNet-18. setting including 100 clients, where 20 clients are ran- We employ cross-entropy as loss function, zero-momentum domly selected in each round. SGD as optimizer, and the Opacus library (version