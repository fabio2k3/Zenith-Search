with γ positive small, one can approximate θ ˙ (t ) and ψ ˙ (t ) by k+1 k k k k+1 k+1 θ θ ψ ψ ˙ k+1 k ˙ k+1 k θ(t ) − , ψ(t ) − . k+1 k+1 (cid:39) γ (cid:39) γ k k This discretization yields the following algorithm,  v ∂ (θ )  k k   ∈ J (cid:16) (cid:17) θ k+1 = θ k +γ k ( β 1 − α)θ k − β 1ψ k − βv k (8)  (cid:16) (cid:17)  ψ = ψ +γ (1 α)θ 1ψ k+1 k k β − k − β k Although the algorithm above is well-defined for our problem, it is not suited to DL. First, the computation of ∂ (θ ) is generally not possible since there is no general operational calculus k J for Clarke’s subdifferential; secondly, a mini-batch strategy must be designed to cope with the large dimension of DL problems which makes the absence of sum rule even more critical. The next section is meant to address these issues and design a practical algorithm. 7 2.3.3 INNA Algorithm and a New Notion of Steady States In order to compute or approximate the subdifferential of at each iteration and to cope with J large data sets, can be approximated by mini-batches, reducing the memory footprint and J computational cost of evaluation. For any B 1,...,N , let us define ⊂ { } (cid:88) : θ l(f(x ,θ),y ). (9) B n n J (cid:55)→ n∈B Unlike in the differentiable case, subgradients do not in general sum up to a subgradient of the (cid:80) sum, that is ∂ (θ) = ∂l(f(x ,θ),y ) in general. To see this, take for example 0 = , J B (cid:54) n∈B n n |·|−|·| the Clarke subgradient of this function at 0 is 0 , whereas ∂( 0 )+∂( 0 ) = [ 1,1]+[ 1,1] = { } | | −| | − − [ 2,2]. Standard DL solvers use backpropagation algorithms which implement smooth calculus − on non-smooth and non-convex objects. Due to the absence of qualification conditions, the resulting objects are not Clarke subgradients in general. In order to match the real-world practice of DL, we introduce a notion of steady states that corresponds to the stationary points generated by a generic mini-batch approach. As we shall see, this allows both for practical applications and convergence analysis (despite the sum rule failure for Clarke subdifferential). We emphasize once more that our goal is to capture the stationary points that are actually met in practice. For any B 1,...,N , we introduce the following objects, ⊂ { } N (cid:88) (cid:88) D = ∂[l(f(x , ),y )], D = ∂[l(f(x , ),y )]. (10) B n n n n J · J · n∈B n=1 Observe that, for each B, we have D ∂ and that is differentiable almost everywhere B B B J ⊃ J J with D = ∂ = , see Clarke (1990). In particular D = ∂ almost everywhere so B B B J J {∇J } J J that the potential differences with the Clarke subgradient occur on a negligible set. When J is tame the equality even hold on the complement of a finite union of manifolds of dimension strictly lower than P—use the classical stratification results for o-minimal structures, Coste (2000). A point satisfying D (θ) 0 will be called D-critical. Note that Clarke-critical J (cid:51) points (0 ∂ ) are D-critical points but that the converse is not true. This terminology is ∈ J motivated by favorable properties: sum and chain rules along curves (see Lemmas 3.2 and 3.3 below) and the existence of a tame Sard’s theorem (see Lemma 3.4). To our knowledge, this notion of a steady state has not previously been used in the literature and a direct approach modeling the mini-batch practice has never been considered before.2 While this notion is needed for the theoretical analysis, one should keep in mind that D is actually what is computed J numerically provided that the automatic differentiation library returns a Clarke subgradient. This computation is usually done with a backpropagation algorithm, similarly to the seminal method of Rumelhart and Hinton (1986). Ultimately, one can rewrite (7) by replacing ∂ by D , which yields a differential inclusion J J adapted to study mini-batch approximations of non-smooth loss functions . This reads, J (cid:40) θ ˙ (t)+βD (θ(t)) +(α 1)θ(t)+ 1ψ(t) 0 J − β β (cid:51) , for a.e. t (0,+ ). (11) ψ ˙ (t) +(α 1)θ(t)+ 1ψ(t) 0 ∈ ∞ − β β (cid:51) Discretizing this system gives a workable version of INNA. Let us consider a sequence (B k ) k∈N of nonempty subsets of 1,...,N ,