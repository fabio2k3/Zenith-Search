sampler for 40 epochs2, using a similarity measure B between embedding vectors for q+,q− various values of (q ,q ). Some are equal to (p ,p ), so that the similarity measure used + − + − for the data generating process and training are identical. Some are greater than (p ,p ), + − so the data generating process still falls within the constraints of the model. Finally, we also let some be less than (p ,p ), in which case the data generating process falls outside + − the specified model class for learning. With the learned embeddings (ω ,...,ω ) we then (cid:98)1 (cid:98)n calculate the value of 1 (cid:88) (cid:12) (cid:12)B (cid:0) ω ,ω (cid:1) −B (Z ,Z ) (cid:12) (cid:12). (27) n2 (cid:12) q+,q− (cid:98)i (cid:98)j p+,p− i j (cid:12) i,j∈[n] In words, we are computing the average L1 error between the estimated edge logits using the learned embeddings (with a bilinear form B between embedding vectors in the loss q+,q− function),andtheactualedgelogitsusedtogeneratethenetwork. Theresultsaredisplayed in Figure 1. By the convergence theorems discussed in Sections 3.2 and 3.4, we expect that (27) will be o (1) if and only if p ≤ q and p ≤ q , and indeed this is the trend displayed p + + − − in Figure 1. For the second result, we illustrate the validity of the sampling formulae calculated in Section4. Todoso, webeginbygeneratinganetworkofnverticesfromoneofthefollowing 2. By epochs, we are referring to the cumulative number of pairs of vertices which are used to form a gradient at each iteration, relative to the total number of edges in the graph. 31 Davison and Austern stochasticblockmodels,whereπ denotesthecommunitysizesandP thecommunitylinkage matrices:   0.7 0.3 0.1 SBM1: π = (1/3,1/3,1/3), P = 0.3 0.5 0.6; 0.1 0.6 0.2   0.75 0.87 0.025 0.81 0.25 0.87 0.93 0.58 0.48 0.45   SBM2: π = (0.1,0.2,0.2,0.3,0.2), P = 0.025 0.58 0.68 0.15 0.48.   0.81 0.48 0.15 0.80 0.92 0.25 0.45 0.48 0.92 0.62 Hereeachvertexisassignedalatentvariableλ ∼ Unif([0,1])whichisusedtodeterminethe i corresponding community (depending on where λ lies within the partition of [0,1] induced i by π). As illustrated in Sections 3 and 4, depending on the sampling scheme (samp), and whether we use a regular or Krein inner product (IP) as the similarity measure B(ω,ω(cid:48)) between embedding vectors (recall Assumption C), there is a function K∗ for which samp,IP the minimizers of (9) satisfy 1 (cid:88) (cid:12) (cid:12) (cid:12)B(ω ,ω )−K∗ (λ ,λ )(cid:12) = o (1). (28) n2 (cid:12) (cid:98)i (cid:98)j samp,IP i j (cid:12) p i,j∈[n] Wenotethatforstochasticblockmodels,whenwechooseB(ω,ω(cid:48)) = (cid:104)ω,ω(cid:48)(cid:105)-corresponding to minimizing I [K] over Z≥0 - we can numerically compute the formula for K∗ via n samp,IP a convex program as a result of Proposition 59. In the case where we choose B(ω,ω(cid:48)) to be a Krein inner product, the discussion in Section 3.2 tells us that we can write down the minima of I [K] over Z exactly. n For each generated network, we train using either a) a random vertex sampler or a random walk + unigram sampler, and b) either the regular or Krein inner product for B(ω,ω(cid:48)). We then calculate the value of (28) for each possible form of K∗ for the samp,IP sampling schemes and inner products we consider. The experiments are then repeated for the same values of n, and number of networks per choice of n, as in the first experiment; the results are displayed in Figure 2. From the figure, we observe that the LHS of (28) decays to zero only when the choice of K∗ corresponds to the sampling scheme and samp,IP inner product actually used, as expected. 5.2 Real data experiments We now demonstrate on real data sets that the use of the Krein inner product leads to improved prediction of whether vertices are connected in a network, and as a consequence can lead to improvements in downstream tasks performance. To do so, we will consider a semi-supervised multi-label node classification task on two different data sets: a proteinproteininteractionnetwork(GroverandLeskovec,2016;Breitkreutzetal.,2008)with3,890 vertices, 76,583 edges and 50 classes; and the Blog Catalog data set (Tang and Liu, 2009) with 10,312 vertices, 333,983 edges and 39 classes. Foreachdataset, weperformthesametypeofsemi-supervisedexperimentsasinVeitch et al. (2018). We learn 128 dimensional embeddings of the networks using two sampling 32 Asymptotics of Network Embeddings Learned via Subsampling Training method: Uniform vertex sampler + regular IP Training method: Uniform vertex sampler + krein IP 1.0 0.3 0.1 Training method: Random walk sampler + regular IP Training method: Random walk sampler + krein IP 1.0 0.3 0.1 300 1000 3000 300 1000 3000 Number of vertices of simulated network stigol hturt dnuorg dna stigol detciderp neewteb rorre 1L .gvA Calculated minima Uniform vertex sampler + regular IP Uniform vertex sampler + krein IP using formula for: Random walk sampler + regular IP Random walk sampler + krein